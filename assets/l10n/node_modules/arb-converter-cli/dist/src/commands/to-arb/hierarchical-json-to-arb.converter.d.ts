export declare class HierarchicalJsonToArbConverter {
    private directoryWithJsonFiles;
    private outputDir;
    constructor(directoryWithJsonFiles: string, outputDir: string);
    convertToArb(): void;
}
